import { Mode } from '../@core/layout/types';

const themeConfig = {
  templateName: 'Xonixlab',
  mode: 'light' as Mode,
  navHidden: false,
  appBar: 'fixed', /* fixed | static | hidden */
  responsiveFontSizes: true,
  disableRipple: false /* true | false */,
};

export default themeConfig
